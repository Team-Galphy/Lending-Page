import styled from "styled-components";

export default function Card(){
    return(
        <Container>

        </Container>
    );
}

const Container = styled.main`
    width: 100vw;
    height: 440px;
`;